# AI 创想家 - 网页前端 UI 与交互设计详述（V1.0）

本文档是 “AI 创想家” 儿童绘本创作平台的核心功能 UI 与交互设计详述。

---

## 全局设计原则

- **视觉风格**：整体采用明亮、饱和度高的色彩，搭配圆角、柔和的线条。图标和按钮设计充满童趣和卡通感。
- **字体**：使用清晰、可读性高且略带可爱的中文字体（如站酷快乐体或类似免费商用字体）。
- **交互反馈**：所有点击、加载、成功、失败操作均有即时、有趣的动态反馈（如轻微缩放、粒子效果、音效等），增强操作感知与愉悦感。

---

## 1. 用户登录 / 注册与 Onboarding 流程

定义新老用户不同进入路径。

### 1.1 新用户注册（Onboarding 引导）

**设计目标**：快速让新用户代入 “创想家” 角色，通过简单引导完成第一次核心体验，降低初始畏难情绪。

#### 页面 1：欢迎与昵称设定

**UI 描述**

- 页面中央显示友好的卡通 AI 助手形象
- AI 助手上方对话气泡：`创想家你好～`
- 对话气泡下方：`该怎么称呼你？`
- 下方输入框，placeholder：`输入你的专属昵称吧`
- 输入框右侧：`随机生成`按钮（骰子图标）
- 最下方：醒目`下一步`按钮，初始灰色不可点击

**交互逻辑**

- 输入框：用户输入昵称后，`下一步`变为高亮可点击；昵称限制 1–10 字符
- 随机生成按钮：
  - `onClick`：填入童趣随机昵称（星际探险家、梦境小画家、故事魔法师等）
  - 点击后`下一步`变为可点击
- 下一步按钮：
  - `onClick`：保存昵称至临时状态，跳转到页面 2

#### 页面 2：引导创作 - 主题选择

**UI 描述**

- 延续 AI 助手 + 对话气泡布局
- 对话气泡：`太棒了，{用户昵称}！接下来让我们马上开始绘本创作吧～`
- 下方文字：`第一步，我们需要有一个完整的故事。你喜欢什么样的故事主题呢？`
- 主题卡片横向排列，带悬浮动效：
  - 校园生活（学校、操场）
  - 科学幻想（火箭、外星人）
  - 童话世界（城堡、公主）
  - 亲情友情（拥抱的小朋友）
  - 自定义主题（问号 / 画笔）

**交互逻辑**

- 主题卡片：
  - `onHover`：卡片轻微放大，描边高亮
  - `onClick`：
    - 前四项：记录主题，跳转到主工作台→故事创作，自动填充主题
    - 自定义主题：记录自定义，跳转到主工作台→故事创作，手动输入主题
- 完成 Onboarding：此步骤后，后续登录直接进入主工作台

### 1.2 老用户登录

**UI 描述**

- 标准登录 / 注册入口，支持：账号密码登录、手机验证码登录、微信扫码登录等方式。

**交互逻辑**

- 用户成功登录后，直接跳转至 主工作台。

---

## 2. 主工作台（老用户登录后首页）

**设计目标**：打造一个集创作、浏览、管理于一体的中心枢纽，让用户可以便捷地在不同功能间切换。

### 2.1 整体布局

采用经典的三栏式布局：顶部导航栏、左侧功能栏、右侧主内容区。

#### 顶部导航栏（Top Bar）

- 固定在页面顶部，背景色稍深于主内容区。
- 内容（从左至右）：
  - Logo: “AI 创想家” 的 Logo。
  - 热门赛事：文字链接。
  - 主题活动：文字链接。
  - 作品与成长：文字链接。
  - 用户头像 (User Avatar): 位于最右侧，圆形，显示用户头像。

#### 左侧功能栏（Left Sidebar）

- 固定在页面左侧，宽度固定。使用大图标 + 文字的形式。
- 内容（从上至下）：
  - 故事创作：默认选中项。
  - 绘本生成:
  - 角色工坊:

#### 右侧主内容区（Main Content Area）

- 占据页面的主要空间，用于显示具体的功能模块。
- 欢迎语：在内容区的最顶部，显示一行醒目的欢迎语：“欢迎 {user.profile.nickname} 宝贝，今天有什么灵感呀？”
- {user.profile.nickname} 需要从用户资料中动态获取。

### 2.2 交互逻辑

- 左侧功能栏:
  - `onClick`：
    - 切换右侧主内容区的显示内容，分别对应 故事创作、绘本生成、角色工坊。
    - 被选中的菜单项需要有明显的高亮状态（如背景色变化、左侧出现色块标记）。
- 顶部导航栏:
  - 热门赛事 `onClick`：跳转至 热门赛事页面。
  - 主题活动 `onClick`：(TBD - 暂定跳转至活动列表页)。
  - 作品与成长 `onClick`：跳转至 作品与成长页面。
  - 用户头像 `onClick`：在头像下方弹出一个下拉菜单（Dropdown），包含 “个人中心” 和 “退出登录” 两个选项。点击 “个人中心” 跳转至 个人中心页面。

---

## 3. 核心功能模块详细设计

### 3.1 故事创作（故事生成器）

**UI 描述**

- 以步骤条（Steps）的形式清晰展示创作流程，共 7 个步骤。当前步骤高亮，已完成步骤显示为 “已完成” 状态。
- 步骤条: `步骤1 → 步骤2 → … → 步骤7`
- 内容区：每个步骤对应不同的表单控件。
- 操作区：底部固定一个 “下一步” 按钮。在最后一步，按钮变为 “开始生成（消耗 XX 积分）”。

#### 步骤详情

* 第 1 步：主题选择 - 页面居中排列 6 个主题卡片（成长激励、校园生活、科学幻想、动物朋友、节日庆典、自定义），每个卡片带对应图标和简短描述，点击选中后卡片变色高亮，底部有 “下一步”“保存草稿” 按钮

* 第 2-6 步：基础设定（主角、场景、任务目标、故事风格、结局） - 统一采用 “问题引导 + 选项卡片” 形式，如 “故事的主角是谁？” 下方排列 5 个角色选项 + 自定义输入框，选择后实时显示 “已选：XX”

* 第 7 步：生成预览 - 显示 “正在生成故事” 加载状态，完成后展示完整故事脚本（分段排版，字体清晰），底部有 “导入绘本生成器”“返回修改”“保存故事” 三个按钮

1. 辅助功能：每步底部固定 “上一步”（第一步隐藏）、“下一步”“保存草稿” 按钮（可使用ctrl+s快捷键），点击 “保存” 后弹出提示 “草稿已保存，可在我的作品 - 草稿箱查看”

#### 生成与结果

- 生成按钮：`开始生成（消耗50积分）`
  - `onHover`：显示提示`生成故事会消耗积分哦～`
  - `onClick`：
    - 按钮变为`生成中…`并显示加载动画（如一个旋转的魔法棒）。
    - 调用生成 API，消耗积分。
    - 生成完成后，当前页面替换为故事结果页。
- 故事结果页：
  - 标题：故事名称。
  - 内容：排版优美的故事正文。
  - 顶部操作栏：
    - 保存按钮：图标为软盘。
    - 导出按钮：图标为下载箭头。
    - 去生成绘本按钮：醒目的彩色按钮，文字旁可加小火箭图标。
  - 交互:
    - 保存: `onClick` 后提示 “已成功保存到我的作品！”。
    - 导出: `onClick` 触发浏览器下载，将故事内容导出为 .docx 文件。
    - 去生成绘本: `onClick` 跳转到 绘本生成 页面，并携带当前故事的 ID。

### 3.2 绘本生成（绘本生成器）

**UI 描述**

- 用户进入此页面时，首先看到两个醒目的、居中放置的选项卡片。

1. **一键导入**：卡片上有 “从我的故事库导入” 的文字，配图为一个书本图标。
2. **外部导入**：卡片上有 “上传本地故事文件” 的文字，配图为一个上传图标。

#### 一键导入模态框

- 标题：“选择要改编的故事”。
- 列表：显示用户已保存的故事列表（故事标题、创建时间）。
- 下方有一个可选项（Checkbox）：“导入已有角色”。
- 底部是 “取消” 和 “确认导入” 按钮。
- 交互：
  - 如果勾选 “导入已有角色”，则在故事列表旁出现角色选择区域，用户可以选择已在 角色工坊 创建的角色。可多选，并可选择导入 “角色设定” 或 “角色立绘”。
  - 选择一个故事并点击 “确认导入”，模态框关闭，进入下一步 “画风选择”。

#### 外部导入

- `onClick` 触发文件选择器，用户可上传 .txt, .doc, .docx 文件。
- 上传成功后，进入下一步 “画风选择”。

#### 画风选择

- UI：以网格形式展示多种画风的预览图。
- 支持画风：
  - 可爱治愈 Q 版风
  - 清新扁平风
  - 水彩手绘风
  - 国风传统画风
  - 拼贴肌理风
  - 美式卡通夸张风
  - 日韩清新风
  - 写实细腻风
  - 复古怀旧风
  - 特殊质感风（蜡笔 / 油画棒 / 彩铅 / 木偶 / 黏土）
- 交互：
  - `onHover`：预览图放大，显示画风名称。
  - `onClick`：选中画风，卡片出现高亮描边。
- 底部显示`开始生成

#### 生成与结果

- 交互：同故事生成，点击后按钮变为加载状态，调用 API，消耗积分。
- 绘本结果页：
  - UI：以画廊（Gallery）或翻页书的形式展示生成的绘本（图文结合）。
  - 顶部操作栏:
    - 保存按钮：保存到 “我的绘本”。
    - 导出按钮: `onClick` 将所有图片和文字打包成一个 .zip 文件下载。

### 3.3 角色工坊（高级功能）

**UI 描述**

- 左侧为角色列表，显示已创建的角色头像和名称。
- 右侧为角色编辑器。
  - 角色设定：姓名、性格、背景故事等文本信息。
  - 角色立绘生成器：可以通过选择特征（发型、眼睛、服装）或输入文字描述来生成角色图片。

**交互逻辑**

- 用户创建并保存角色后，该角色即可在 “绘本生成” 时被导入。

---

## 4. 顶部导航栏功能页面

### 4.1 热门赛事

**UI 描述**

- 页面标题：“热门赛事”。
- 下方以卡片列表形式展示各个赛事。
- 每个卡片包含：
  - 赛事 Logo / 主视觉图。
  - 赛事名称（如：全国青少年人工智能辅助生成数字艺术创作者大赛）。
  - 主办单位（如：文化和旅游部艺术发展中心）。
  - 一个 “查看详情” 的按钮。

**交互逻辑**

- 点击卡片或 “查看详情” 按钮，`onClick` 在新标签页打开对应的赛事介绍 PDF 文件。

### 4.2 作品与成长

**UI 描述**

- 页面采用标签页（Tabs）布局，分为 “我的作品” 和 “成长报告” 两个标签。

#### 我的作品 (默认 Tab)

- 顶部显示作品统计：“共创作了 {X} 个故事，{Y} 本绘本，{Z} 个角色”。
- 下方分区域展示 “我的故事”、“我的绘本”、“我的角色”、“已发布作品” 的卡片列表。

#### 成长报告 (Tab)

- UI 布局：采用信息卡片和数据可视化的方式。
  - 加入创想家： “你已经加入创想家 {days} 天了！”
  - 我的成就： 以 “徽章墙” 的形式展示。
    - 创作 1 个故事 -> “故事新星” 徽章
    - 创作 10 个故事 -> “故事大王” 徽章
    - ...（以此类推，为绘本、角色也设置不同等级的徽章）
  - 创作偏好： 雷达图，展示用户在 “校园、科幻、童话” 等主题上的创作次数分布。
  - 闪光点挖掘： 根据故事内容分析出的高频词汇，如 “勇敢”、“探索” 等。
  - 推荐创作： “我们发现你很喜欢科幻主题，要不要试试创作一个关于‘火星移民’的故事？”
  - 性格分析 (MBTI)： （高级功能）根据创作内容和偏好，给出一个娱乐性的性格分析报告。
  - 可参与赛事推荐： 根据用户的作品质量和主题，推荐匹配的赛事。
  - 作品优化建议： 智能分析某个作品，给出 “可以尝试增加更多对话来让角色更生动哦” 之类的建议。

---

## 5. 个人中心（点击用户头像）

**UI 描述**

- 典型的设置页面布局，左侧为菜单，右侧为内容。
- 左侧菜单：个人资料、管理订阅、家长模式、意见反馈。

### 5.1 个人资料

**UI**：包含以下字段的表单。

- 姓名：输入框
- 年级：下拉选择框（一年级、二年级...）
- 学校：输入框
- 底部有 “保存” 按钮。

### 5.2 管理订阅

**用户付费方式**：选择您的计划以解锁功能

左右对比定价表：

- **免费版**：
  - 写故事：10 次
  - 创绘本：10 次
  - 立角色：10 次
- **会员版**：
  - 39 元 / 月
  - 所有功能均解锁
  - 每周更新成长报告
  - 智能推荐可参与热门赛事与活动
  - 作品创作建议
  - 支付方式：微信支付、支付宝支付
- 立即开通：弹出支付框（微信支付二维码、支付宝支付二维码 / 选项）

### 5.3 家长模式

**UI**：

- 进入此页面前，需要先通过一个模态框进行身份验证。
- 验证模态框:
  - 标题：“请输入家长密码”
  - 4 个并排的密码输入框，只能输入数字。
  - 输入错误有提示。
- 家长模式页面:
  - 包含各种安全管控的开关（Toggle）。如：每日使用时长限制、禁用自定义主题、消费前需验证密码等。

### 5.4 意见反馈

**UI**：一个标准的反馈表单。

- 作品评分: 5 颗星的评分组件。
- 问题描述：多行文本框（Textarea），placeholder 为 “告诉我们你的想法或遇到的问题...”。
- 上传照片：最多可上传 3 张截图。
- 联系您的方式：输入框，placeholder 为 “电话 / 邮件”。
- “提交” 按钮。

**交互逻辑**：

- 点击 “提交” 后，按钮变为 “提交中…”，成功后显示 “感谢你的反馈！” 的提示。

---

> （注：文档部分内容可能由 AI 生成）

